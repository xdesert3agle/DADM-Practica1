<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="es.dadm.miguelangelvicentebaeza.practica1.MainActivity">

    <TextView
        android:id="@+id/welcome_msg"
        android:layout_centerHorizontal="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:textAlignment="center"
        android:textSize="22sp"
        android:text="@string/welcome_msg" />

    <EditText
        android:layout_below="@id/welcome_msg"
        android:id="@+id/txt_name"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:ems="10"
        android:inputType="textPersonName"
        android:hint="@string/txt_name_content" />

    <ImageView
        android:id="@+id/img_android"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/txt_name"
        android:layout_width="262dp"
        android:layout_height="220dp"
        app:srcCompat="@drawable/androidlogo" />

    <Button
        android:id="@+id/btn_start"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/img_android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_start_value" />
</RelativeLayout>
